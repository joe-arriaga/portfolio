# Todo App
## Instructions to Run
Run 'make'

- The program requires the Data.Split library,
  The Makefile includes a command that should ask you to install it but you may
  have to install it manually if it causes trouble.
  After that, simply run 'runhaskell Main.hs'

